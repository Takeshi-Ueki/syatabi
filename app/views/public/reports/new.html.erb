<div class="row main-top">
  <div class="col-11 col-md-10 mx-auto">
    <div class="row">
      <div class="col-12 text-right">
        <%= link_to "戻る", user_path(@user), class: "btn btn-sm mr-3" %>
      </div>
    </div>

    <%= form_with model: [@user, @report] do |f| %>

      <div class="form-group">
        <%= f.label :reason, "理由(255文字まで)" %>
        <%= f.text_area :reason, rows: 5, maxlength: 255, class: "form-control" %>
      </div>

      <div class="form-group my-5">
        <%= f.label :url, "問題のある投稿やコメントのURLがあれば入力してください" %>
        <%= f.text_field :url, maxlength: 255, class: "form-control" %>
      </div>

      <div class="text-center">
        <%= f.submit "通報する", class: "btn btn-primary mt-3" %>
      </div>

    <% end %>
  </div>
</div>
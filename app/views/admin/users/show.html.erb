<div class="row main-top">
  <div class="col-12 col-lg-8 mx-lg-auto">
    <div class="row py-4">
      <div class="col-12 col-md-6 col-xl-5 text-center">
        <div>
          <%= image_tag @user.get_profile_image(150, 150), class: "profile-img" %>
        </div>
        <div class="py-3"><%= @user.name %></div>
      </div>
      <div class="col-12 col-md-6 col-xl-7">
        <% if @user.profile.blank? %>
          プロフィールを設定していません
        <% else %>
          <%= @user.profile %>
        <% end %>
      </div>
    </div>

    <div class="row">
      <div class="col-12 text-center user-status">
        <% if @user.is_active %>
          <strong class="user-true">有効</strong>
        <% else %>
          <strong class="user-false">無効</strong>
        <% end %>
        なユーザーです
      </div>
      <div class="col-12 mb-5 py-5 text-center">
          <%= link_to "編集する", edit_admin_user_path, class: "btn btn-success btn-lg" %>
      </div>
    </div>
    <div class="row">
      <div class="col-12">
        <% @posts.each do |post| %>
          <%= link_to admin_post_path(post.id) do %>
            <div class="post-list mb-5">
              <div class="p-3">
                <% post.images.each do |image| %>
                  <%= image_tag image.variant(resize_to_limit: [200, 200]) %>
                <% end %>
              </div>
              <div class="p-3"><%= post.body %></div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>